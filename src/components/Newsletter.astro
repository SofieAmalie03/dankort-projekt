<section class="newsletter-section">
  <div class="newsletter-image">
    <img src="/nyhedsbillede.webp" alt="Nyhedsbrev" />
  </div>

  <div class="newsletter-content">
    <h2>Vil du med næste gang?</h2>
    <p>Få besked når nye events, pop-ups og oplevelser lander i din by.</p>
  </div>

  <form class="newsletter-form">
    <input type="email" placeholder="Din e-mail" />
    <button type="submit">Tilmeld mig →</button>
  </form>
</section>

<style>
  .newsletter-section {
    margin-top: 40px;
    display: grid;
    grid-template-columns: 1fr 1.4fr 1.6fr;
    align-items: center;
    background-color: var(--green-soft);
  }

  .newsletter-image img {
    width: 100%;
    height: 150px;
    object-fit: cover;
  }

  .newsletter-content {
    padding-left: 80px;
  }

  .newsletter-content h2 {
    font-size: 24px;
    margin-bottom: 8px;
  }

  .newsletter-content p {
    font-size: 14px;
  }

  .newsletter-form {
    display: flex;
    justify-content: flex-end;
    padding-right: 60px;
  }

  .newsletter-form input {
    width: 260px;
    padding: 18px 20px;
    border: none;
    border-radius: 999px 0 0 999px;
  }

  .newsletter-form button {
    padding: 18px 28px;
    border: none;
    background-color: var(--green-dark);
    color: white;
    border-radius: 0 999px 999px 0;
    font-weight: 700;
    cursor: pointer;
  }

  @media (max-width: 1000px) {
    .newsletter-section {
      grid-template-columns: 1fr;
    }

    .newsletter-image img {
      height: 220px;
    }

    .newsletter-content {
      padding: 28px 24px 12px;
    }

    .newsletter-form {
      padding: 0 24px 28px;
      justify-content: flex-start;
    }
  }

  @media (max-width: 700px) {
    .newsletter-section {
      margin-top: 7px;
      display: grid;
      grid-template-columns: 1fr;
      padding: 12px 20px 14px;
      background-color: var(--green-soft);
    }

    .newsletter-image {
      display: none;
    }

    .newsletter-content {
      padding: 0 0 8px;
    }

    .newsletter-content h2 {
      font-family: Arial, Helvetica, sans-serif;
      font-size: 13px;
      margin-bottom: 4px;
      color: var(--green-dark);
    }

    .newsletter-content p {
      font-size: 8.5px;
      line-height: 1.3;
      max-width: 220px;
    }

    .newsletter-form {
      padding: 0;
      display: grid;
      grid-template-columns: 1fr;
      gap: 7px;
    }

    .newsletter-form input {
      width: 100%;
      padding: 10px 13px;
      border-radius: 999px;
      font-size: 9px;
    }

    .newsletter-form button {
      width: 100%;
      padding: 10px 13px;
      border-radius: 999px;
      font-size: 9px;
      text-align: center;
    }
  }
</style>